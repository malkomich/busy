# APP BASICS
spring.application.name = buSy
web.rootUrl = http://localhost:8080

# SPRING MVC (WebMvcProperties)
spring.mvc.favicon.enabled = true
spring.mvc.view.prefix = /
spring.mvc.view.suffix = .jsp

# Disable error default pages
server.error.whitelabel.enabled = true

# DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url = @db.connectionURL@
spring.datasource.username = @db.username@
spring.datasource.password = @db.password@
spring.datasource.driverClassName = @db.driverClass@
spring.datasource.sql-script-encoding = UTF-8

# Email (MailProperties)
spring.mail.host = smtp.gmail.com
spring.mail.port = 465
spring.mail.username = busy.validation@gmail.com
spring.mail.password = b1u2s3y4#SeCuRePASSWORD
spring.mail.default-encoding = UTF-8
spring.mail.properties.mail.smtps.auth = true
spring.mail.properties.mail.smtps.ssl.trust = smtp.gmail.com
spring.mail.properties.mail.smtps.starttls.enable = true
spring.mail.properties.mail.smtps.timeout = 15000
spring.mail.properties.mail.smtps.socketFactory.class = javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtps.socketFactory.fallback = false
spring.mail.properties.mail.transport.protocol = smtps
spring.mail.properties.mail.debug = true
spring.mail.protocol= smtps

# INTERNATIONALIZATION (MessageSourceAutoConfiguration)
spring.messages.encoding = UTF-8
spring.messages.fallback-to-system-locale = true

# HTTP encoding (HttpEncodingProperties)
spring.http.encoding.charset = UTF-8
spring.http.encoding.enabled = true
spring.http.encoding.force = true
spring.mandatory-file-encoding = UTF-8
server.tomcat.uri-encoding = UTF-8
